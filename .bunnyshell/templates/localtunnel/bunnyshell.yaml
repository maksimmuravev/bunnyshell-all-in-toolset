kind: Environment
name: chisel
type: primary
components:
    -
        kind: Service
        name: chisel
        dockerCompose:
            image: "jpillora/chisel:latest"
            command: "server"
            ports:
              - '8080:8080'
        hosts:
            -
                hostname: 'chisel-{{ env.base_domain }}'
                path: /
                servicePort: 8080
